# Copy to web/server/.env and fill in.
# .env is gitignored; never commit real secrets.

# Hosted AI design service (Phase 3)
TAMARIND_API_KEY=""
TAMARIND_API_URL="https://api.tamarind.bio"

# Optional: override Upside install location. The server auto-detects from
# the repo root, so this only matters if you have multiple checkouts.
# UPSIDE_HOME="/workspaces/DynaLab-merge-dynalab"
